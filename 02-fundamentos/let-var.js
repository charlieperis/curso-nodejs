var nombre = charlie
var nombre = charlie2
var nombre = charlie3

// con "var" (o variables globales) la variable toma el valor de la ultima, en este caso charlie 3.
// Es decir se re escribe (se pisa) el valor delcarado.

let nombre = charlie

// con let solo solo se puede declarar una vez con es enombre de variable.